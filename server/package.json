{
  "name": "cppplay-server",
  "version": "1.0.0",
  "private": true,
  "description": "创玩 · C++ 创作平台后端 —— Node.js + Express + 内置 SQLite + git + Emscripten 构建",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "start:all": "node src/start-all.js",
    "dev": "node --watch src/index.js",
    "test": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "dockerode": "^5.0.1",
    "express": "^4.21.2",
    "http-proxy-middleware": "^3.0.5",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^9.0.5",
    "tar-stream": "^3.2.0"
  }
}
